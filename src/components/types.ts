// Define the Model interface for model data structure
export interface Model {
    name: string;
    description: string;
    type: string;
    tags: string[];
}

// Define the Tool interface for tool data structure
export interface Tool {
    name: string;
    description: string;
    type: string;
    tags: string[];
}